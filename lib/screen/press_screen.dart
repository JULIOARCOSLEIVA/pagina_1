import 'package:flutter/material.dart';

class PressScreen extends StatelessWidget {
  const PressScreen({super.key});
  @override
  Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(
      title: const Text('PressScreen'),
    ),
    body: const Center(
      child: Text('PressScreen'),
    ),
    );
  }
}